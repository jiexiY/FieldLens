{
  "action": "rules.insert",
  "value": {
    "name": "FieldLens voice request limit",
    "description": "Limit only POST /api/voice to 30 requests per minute per IP; leave public environmental data and pages unchanged.",
    "active": true,
    "conditionGroup": [{"conditions": [{"type":"path","op":"eq","value":"/api/voice"},{"type":"method","op":"eq","value":"POST"}]}],
    "action": {"mitigate": {"action":"rate_limit","rateLimit":{"algo":"fixed_window","window":60,"limit":30,"keys":["ip"],"action":"rate_limit"}}}
  }
}
